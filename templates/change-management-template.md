# OpenShift 変更管理票

> [!NOTE]
> 実務成果物例を組織の正式様式へ転記する際に使用する空テンプレートです。`<...>` は案件固有値へ置換し、承認済み標準と対象版の公式資料を優先してください。

## 1. 変更概要

| 項目 | 内容 |
|---|---|
| 変更 ID／件名 | `<記入>` |
| 対象クラスタ／Project | `<記入。要確認>` |
| 種別 | `標準／通常／緊急（定義は要確認）` |
| 希望日時／保守時間 | `<記入>` |
| 起票／実施／確認／承認 | `<役割>` |
| 関連 Git commit／文書 | `<記入>` |

## 2. 目的・変更内容

- 背景／目的: `<なぜ必要か>`
- 変更前: `<現在値・構成>`
- 変更後: `<目標値・構成>`
- 対象外: `<記入>`
- Secret の扱い: `<参照 ID のみ。実値は記載しない>`

## 3. 影響・リスク

| 観点 | 影響／リスク | 可能性 | 重大度 | 軽減策 |
|---|---|---|---|---|
| API／Ingress | `<記入>` | `<低中高>` | `<低中高>` | `<記入>` |
| Workload／PVC | `<記入>` | `<値>` | `<値>` | `<記入>` |
| Operator／更新 | `<記入>` | `<値>` | `<値>` | `<互換性確認>` |
| 監視／ログ | `<記入>` | `<値>` | `<値>` | `<記入>` |

- 利用者停止: `<有無、時間、対象>`
- 同時変更／競合: `<変更 ID またはなし>`

## 4. 実施計画

| 時刻 | No. | 操作 | 実施者 | 期待結果 | 所要 |
|---|---:|---|---|---|---|
| `<時刻>` | 1 | `oc whoami --show-server` | `<役割>` | 対象クラスタ一致 | `<分>` |
| `<時刻>` | 2 | `oc diff -f <path>` | `<役割>` | 承認済み差分のみ | `<分>` |
| `<時刻>` | 3 | `<適用操作>` | `<役割>` | `<期待結果>` | `<分>` |

## 5. 検証・監視

| 試験 ID | 確認内容 | 合格条件 | 監視期間 | 証跡 |
|---|---|---|---|---|
| VT-001 | `oc get co` | 変更起因の異常なし | `<分>` | `<保管先>` |
| VT-002 | `<業務疎通>` | `<SLO>` | `<分>` | `<保管先>` |

## 6. 中止・切戻し

- Go/No-Go 判断者／時刻: `<記入>`
- 中止条件: `<重大アラート、規定時間超過など>`
- 切戻し可能期限: `<時刻>`
- ロールバック計画書／バックアップ ID: `<リンク>`
- 切戻し後の確認: `<試験 ID>`

## 7. 連絡・承認

| 対象 | 事前連絡 | 開始／完了連絡 | 連絡経路 |
|---|---|---|---|
| `<運用／利用者>` | `<日時>` | `<条件>` | `<記入>` |

| 役割 | 氏名 | 判定 | 日付 | コメント |
|---|---|---|---|---|
| 変更承認者 | `<記入>` | `要確認` | `<日付>` | `<記入>` |

## 8. 実績

- 結果: `成功／切戻し／保留`
- 開始／終了: `<時刻>`
- 想定との差: `<なし／内容>`
- 残課題: `<ID>`
