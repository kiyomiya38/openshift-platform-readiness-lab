# OpenShift 基本設計書

> 使い方: `<記入>` を案件情報へ置き換える。未確定事項は推測せず `要確認（担当／期限）` と記載する。Secret の実値は記載しない。

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 案件名 | `<記入>` |
| 対象環境 | `開発／検証／本番（要確認）` |
| OpenShift バージョン | `要確認` |
| 基盤 | `オンプレミス／ROSA／ARO／その他（要確認）` |
| 作成・レビュー・承認 | `<氏名または役割>` |
| 版・更新日 | `0.1／YYYY-MM-DD` |

## 2. 目的・範囲

- 目的: `<提供する業務価値>`
- 対象: `<クラスタ、Project、外部接続>`
- 対象外: `<アプリ内部設計など>`
- 前提・制約: `<保守時間、予算、既存標準>`

## 3. 要件対応

| 要件 ID | 要件 | 設計方針 | 確認方法 | 状態 |
|---|---|---|---|---|
| REQ-001 | `<可用性など>` | `<方針>` | `<試験 ID>` | `要確認` |

## 4. クラスタ構成

| 項目 | 設計値 | 根拠・注意 |
|---|---|---|
| クラスタ名／Base Domain | `<記入>` | DNS 命名規則を確認 |
| Control Plane | `<台数・CPU・メモリ>` | 障害ドメインは要確認 |
| Worker／Infra Node | `<台数・サイジング>` | MachineSet 利用可否は要確認 |
| 可用性ゾーン／ラック | `<配置>` | 同時障害範囲を明記 |
| 更新チャネル | `<stable-x.y など>` | 保守ポリシーと整合 |

## 5. ネットワーク・名前解決

| 項目 | 設計値 | 確認事項 |
|---|---|---|
| API VIP／Ingress VIP | `<記入>` | LB ヘルスチェック方式は要確認 |
| API／Apps DNS | `api.<cluster>.<domain>` / `*.apps.<cluster>.<domain>` | 正逆引き・TTL |
| Machine／Pod／Service CIDR | `<記入>` | 既存網との重複禁止 |
| CNI | `OVN-Kubernetes など（要確認）` | バージョン依存 |
| Egress／Proxy | `<許可先・方式>` | 非接続環境は別紙参照 |
| NetworkPolicy | `default deny の適用方針（要確認）` | DNS・監視通信を考慮 |
| Firewall | `<送信元、宛先、ポート、方向>` | 公式ポート一覧と照合 |

## 6. 認証・認可・セキュリティ

- Identity Provider: `<LDAP/OIDC/HTPasswd 等。要確認>`
- 管理者グループと職務分離: `<記入>`
- RBAC: Project 単位の最小権限を原則とする。
- SCC: 標準 SCC を優先し、カスタム SCC は理由・対象 SA・承認者を記録する。
- 証明書: `<発行元、SAN、更新責任、期限監視>`
- Secret: `<外部 Secret 管理／暗号化／ローテーション方針>`（実値は記載しない）
- 監査ログ保存: `<保存先・期間。要確認>`

## 7. ストレージ

| 用途 | StorageClass | AccessMode | 容量／性能 | 暗号化・バックアップ |
|---|---|---|---|---|
| Registry | `<記入>` | `RWO/RWX` | `<記入>` | `<記入>` |
| アプリ | `<記入>` | `<記入>` | `<IOPS 等>` | `<記入>` |

- CSI／ODF の提供範囲と責任分界: `要確認`
- デフォルト StorageClass: `<記入>`

## 8. 運用・非機能

| 分類 | 方針 | 目標・担当 |
|---|---|---|
| 監視・通知 | Cluster Monitoring／利用者ワークロード監視 | `<SLO、通知先>` |
| ログ | `<Loki 等への収集対象>` | `<保存期間>` |
| バックアップ | `<OADP、etcd、外部ストレージ>` | `<RPO/RTO>` |
| 更新 | `<事前検証、承認、ロールバック判断>` | `<保守時間>` |
| 容量 | `<閾値と増設リードタイム>` | `<担当>` |

## 9. Operator・アドオン

| Operator | Namespace | チャネル／承認方式 | 用途 | 互換性確認 |
|---|---|---|---|---|
| `<名称>` | `<記入>` | `<記入>` | `<記入>` | `要確認` |

## 10. 試験・移行・残課題

- 受入条件: `<正常性、性能、障害、バックアップ復元>`
- 移行方針: `<新規／段階移行／切替条件>`
- ロールバック方針: `<判断者、期限、復旧点>`

| 課題 ID | 内容 | 影響 | 担当 | 期限 | 状態 |
|---|---|---|---|---|---|
| ISS-001 | `<要確認事項>` | `<影響>` | `<役割>` | `<日付>` | `Open` |

## 11. 参照・承認

- [OpenShift Container Platform ドキュメント](https://docs.redhat.com/en/documentation/openshift_container_platform/)
- 適用する製品バージョンの公式文書と組織標準をレビュー時に固定する。

| 役割 | 氏名 | 判定 | 日付 | コメント |
|---|---|---|---|---|
| 基盤責任者 | `<記入>` | `要確認` | `<日付>` | `<記入>` |
