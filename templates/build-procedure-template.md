# OpenShift 構築手順書

> [!NOTE]
> 実務成果物例を組織の正式様式へ転記する際に使用する空テンプレートです。`<...>` は案件固有値へ置換し、承認済み標準と対象版の公式資料を優先してください。

> 検証済みの対象バージョン、実行者、戻り値を明確にする。例示コマンドのプレースホルダーを置換し、まず非本番で確認する。

## 1. 作業情報

| 項目 | 内容 |
|---|---|
| 作業名／変更 ID | `<記入>` |
| 対象クラスタ／環境 | `<記入。要確認>` |
| 作業日時／停止影響 | `<記入>` |
| 実施／確認／承認 | `<役割>` |
| 使用端末／踏み台 | `<記入>` |
| 対象 OpenShift／oc 版 | `要確認` |

## 2. 安全条件

- [ ] `oc whoami --show-server` で対象 API を確認した。
- [ ] `oc whoami` と必要最小権限を確認した。
- [ ] 本番作業の承認・連絡・保守時間を確認した（該当時）。
- [ ] バックアップ／エクスポートと復元可能性を確認した。
- [ ] Secret 実値を端末履歴・ログ・本書へ残さない。
- [ ] 中止条件とロールバック責任者を共有した。

## 3. 前提・資材

| ID | 前提／資材 | 版・ハッシュ／場所 | 確認方法 | 結果 |
|---|---|---|---|---|
| PRE-001 | Manifest | `<Git commit/path>` | `git rev-parse HEAD` | `要確認` |
| PRE-002 | API 到達性 | `<FQDN>` | `oc whoami --show-server` | `要確認` |

## 4. 事前確認

| No. | 操作 | 期待結果 | 実績／時刻 |
|---:|---|---|---|
| 1 | `oc get clusterversion` | 想定バージョン、更新中でない | `<記録>` |
| 2 | `oc get co` | 対象外の異常がない、または既知事象 | `<記録>` |
| 3 | `oc get nodes` | 想定ノードが Ready | `<記録>` |
| 4 | `oc get events -A --sort-by=.lastTimestamp` | 作業影響となる異常がない | `<記録>` |

## 5. 構築手順

| No. | 実行者 | コマンド／操作 | 期待結果 | 判定・証跡 | 失敗時 |
|---:|---|---|---|---|---|
| 1 | `<役割>` | `oc diff -f <manifest-dir>` | 意図した差分だけ表示 | `<保存先>` | 差分レビューへ戻る |
| 2 | `<役割>` | `oc apply --dry-run=server -f <manifest-dir>` | validation 成功 | `<保存先>` | 適用しない |
| 3 | `<役割>` | `oc apply -f <manifest-dir>` | configured/created | `<保存先>` | 中止基準を評価 |
| 4 | `<役割>` | `oc rollout status deployment/<name> -n <project> --timeout=<duration>` | rollout 完了 | `<保存先>` | ロールバック手順へ |

> クラスタ全体に影響する削除、強制操作、MachineConfig 変更は個別手順・承認・影響評価を追加する。

## 6. 検証

| 試験 ID | 確認 | コマンド例 | 期待結果 | 実績 |
|---|---|---|---|---|
| VT-001 | Pod | `oc get pod -n <project> -o wide` | Ready | `<記録>` |
| VT-002 | Service | `oc get endpointslice -n <project> -l kubernetes.io/service-name=<service>` | Endpoint あり | `<記録>` |
| VT-003 | Route | `oc get route/<name> -n <project>` | Host/Admitted 正常 | `<記録>` |

## 7. 中止・ロールバック

- 中止条件: `<例: 重大アラート、新規 Pod が規定時間内に Ready にならない>`
- 判断者／判断期限: `<記入>`
- ロールバック手順書: `<リンク／版>`
- ロールバック後の確認: `<試験 ID>`

## 8. 完了記録

| 項目 | 内容 |
|---|---|
| 開始／終了 | `<時刻>` |
| 実施結果 | `成功／切戻し／保留` |
| 差分・Git commit | `<記入>` |
| 証跡保管先 | `<アクセス制御された場所>` |
| 残課題 | `<課題 ID またはなし>` |
