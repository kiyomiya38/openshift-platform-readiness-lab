# OpenShift 障害調査報告書

> 観測した「事実」、まだ検証していない「仮説」、実施した「操作」を分ける。顧客情報、token、Secret、個人情報は記録しない。

## 1. 概要

| 項目 | 内容 |
|---|---|
| 事象 ID／件名 | `<記入>` |
| 対象環境 | `<マスク済み cluster/project>` |
| 検知／復旧日時 | `<日時／継続中>` |
| 影響 | `<利用者・機能・範囲・継続時間>` |
| Severity | `<組織定義。要確認>` |
| 現在状態 | `調査中／暫定復旧／恒久対応済み` |

## 2. 事象と期待状態

- 期待状態: `<SLO／設計上の状態>`
- 実際の状態: `<エラー、状態、再現条件>`
- 初回検知方法: `<Alert／問い合わせ／試験>`
- 直前変更: `<変更 ID／なし／要確認>`

## 3. タイムライン

| 時刻 | 事実／操作 | 実施者 | 結果・判断 |
|---|---|---|---|
| `<日時>` | `<検知事実>` | `<役割>` | `<記入>` |

## 4. 初動確認

| レイヤー | コマンド例 | 観測事実 | 証跡 |
|---|---|---|---|
| Cluster | `oc get clusterversion; oc get co` | `<記入>` | `<リンク>` |
| Node | `oc get nodes; oc describe node <name>` | `<記入>` | `<リンク>` |
| Workload | `oc get pod -n <ns>; oc describe pod <name> -n <ns>` | `<記入>` | `<リンク>` |
| Log/Event | `oc logs <pod> -n <ns>; oc get events -n <ns> --sort-by=.lastTimestamp` | `<記入>` | `<リンク>` |
| Network | `oc get svc,endpointslice,route -n <ns>` | `<記入>` | `<リンク>` |
| Storage | `oc get pvc,pv -n <ns>` | `<記入>` | `<リンク>` |

## 5. 切り分け

| 仮説 ID | 仮説 | 支持する事実 | 反証／不足 | 次の安全な確認 | 判定 |
|---|---|---|---|---|---|
| H-001 | `<原因候補>` | `<事実>` | `<事実>` | `<read-only 確認>` | `未確認` |

## 6. 原因

- 直接原因: `<確定していなければ「要確認」>`
- 根本原因: `<人ではなく仕組み・条件を記載>`
- 根拠: `<ログ、設定差分、再現／復旧試験>`
- 影響が限定／拡大した理由: `<防御層・監視・依存関係>`

## 7. 対応

| 種別 | 内容 | リスク・承認 | 結果 | 変更 ID |
|---|---|---|---|---|
| 暫定 | `<操作>` | `<記入>` | `<実績>` | `<ID>` |
| 恒久 | `<設定・設計変更>` | `<記入>` | `<実績>` | `<ID>` |

## 8. 復旧確認

| 確認 | 期待結果 | 実績 | 監視期間 | 判定 |
|---|---|---|---|---|
| ClusterOperator／Node | 正常 | `<記入>` | `<分>` | `<判定>` |
| 業務疎通 | `<基準>` | `<記入>` | `<分>` | `<判定>` |
| 再発 | 該当 Alert/Event なし | `<記入>` | `<分>` | `<判定>` |

## 9. 再発防止・残課題

| Action | 種別 | Owner | 期限 | 完了条件 | 状態 |
|---|---|---|---|---|---|
| ACT-001 | `設計／監視／試験／手順` | `<役割>` | `<日付>` | `<測定可能な条件>` | `Open` |

## 10. 取扱い・レビュー

- 証跡は Authorization header、cookie、token、Secret 値をマスキングした。
- ベンダーへ提供する must-gather 等は組織の情報持出し手続きを要確認。

| 役割 | 氏名 | 判定 | 日付 |
|---|---|---|---|
| 技術レビュー | `<記入>` | `要確認` | `<日付>` |
