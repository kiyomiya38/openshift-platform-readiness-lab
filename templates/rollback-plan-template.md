# OpenShift ロールバック計画書

## 1. 対象・目的

| 項目 | 内容 |
|---|---|
| 変更／移行 ID | `<記入>` |
| 対象環境・リソース | `<cluster/project/kind/name。要確認>` |
| 復旧目標 | `<既知の正常構成／時点>` |
| RPO／RTO | `要確認` |
| 判断者／実施者／確認者 | `<役割>` |

## 2. ロールバック可能性

| 変更対象 | 方式 | 前提 | データ影響 | 可逆性 |
|---|---|---|---|---|
| Manifest | `<直前 Git commit を再適用>` | API 互換性 | `<記入>` | `要確認` |
| Operator／CRD | `<製品手順>` | downgrade 対応可否 | 高い可能性 | `公式文書で要確認` |
| PVC／DB | `<snapshot/backup restore>` | 復元試験済み | 差分消失 | `要確認` |
| VM 移行 | `<移行元再開>` | 書込み整合 | split-brain 防止 | `要確認` |

> OpenShift／Operator の downgrade は単純な Manifest 差し戻しではない。対象版の公式手順とサポート条件を必ず確認する。

## 3. 判断基準

- Trigger: `<重大アラート、疎通失敗、性能閾値、時間超過>`
- 判断期限: `<時刻。越えるとデータ同期等により切戻し困難>`
- 連絡・承認: `<変更責任者、業務責任者、ベンダー窓口>`
- ロールフォワードを選ぶ条件: `<短時間修正の方が安全な場合>`

## 4. 事前準備

- [ ] `oc whoami --show-server` で対象を確認した。
- [ ] Git commit／エクスポート／バックアップ ID を記録した。
- [ ] 復元試験結果 `<ID>` を確認した。
- [ ] 監視、利用者連絡、書込み停止を準備した。
- [ ] Secret は安全な保管先から取得し、本書へ転記しない。

## 5. 手順

| No. | 操作 | 実施者 | 期待結果 | 中止時 |
|---:|---|---|---|---|
| 1 | 変更停止・影響範囲隔離 | `<役割>` | 追加変更なし | 責任者へ連絡 |
| 2 | 現状証跡取得 | `<役割>` | resource/event/log を保全 | 取得不能を記録 |
| 3 | `<承認済み復元操作>` | `<役割>` | 既知の構成へ戻る | サポートへ連絡 |
| 4 | `oc get co && oc get nodes` | `<役割>` | 変更起因の異常なし | 復旧判断へ |
| 5 | `<業務疎通・データ整合確認>` | `<役割>` | 合格条件達成 | 継続隔離 |

## 6. ロールバック後確認

| 試験 ID | 確認 | 期待結果 | 監視時間 | 証跡 |
|---|---|---|---|---|
| RB-001 | API／Ingress | `<正常応答>` | `<分>` | `<保管先>` |
| RB-002 | Pod／Operator | `<Ready/Available>` | `<分>` | `<保管先>` |
| RB-003 | Data | `<整合性条件>` | `<分>` | `<保管先>` |

## 7. 実績・事後対応

- 判断時刻／判断理由: `<記入>`
- 開始／終了／結果: `<記入>`
- 失われた可能性のあるデータ: `<なし／範囲／要確認>`
- 原因調査・再実施条件: `<課題 ID>`
- 設計／手順／監視の更新: `<担当・期限>`
